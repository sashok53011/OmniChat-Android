/* secrets.h — copy this file to secrets.h and fill in your real values.
 * secrets.h is NOT committed (see .gitignore).
 */
#pragma once

#define APP_WIFI_SSID              "your-wifi-ssid"
#define APP_WIFI_PASSWORD          "your-wifi-password"
#define PROVIDER_OPENCODE_API_KEY  "your-opencode-api-key"
